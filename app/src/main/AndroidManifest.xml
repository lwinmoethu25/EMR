<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lovespectre.lwin.emr" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add Product Activity -->
        <activity android:name=".NewPatient" android:label="Add New Patient">
        </activity>



        <!-- All Patient Activity -->
        <activity android:name=".ShowAllPatient" android:label=" Patient List">
    </activity>

        <!-- All Patient Activity --><activity android:name=".UpdatePatient" android:label=" Patient Info">
    </activity>

        <activity android:name=".Preferences" android:label="Setting"></activity>

    </application>

    <!--  Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>



</manifest>
